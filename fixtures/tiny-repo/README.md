# Tiny Repo

A minimal Go web service for testing CodeDoc.

## Features

- Simple HTTP server
- Basic API endpoints
- Health check endpoint

## Running

```bash
go run main.go
```

## Endpoints

- GET `/` - Home page
- GET `/api/users` - List users
- GET `/health` - Health check